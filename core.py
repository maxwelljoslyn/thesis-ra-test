# this is the "core function" we discussed

def all_implications(implications, premises):
    """Given a key-value map {fact: implied_facts},
    and an iterable of logical premises (facts which we assume are true),
    return the set of all "new" facts: the implications of all premises, EXCEPT any of those which were included in the premises.
    SEE TESTS FOR EXAMPLES.
    """
    pass

# you can write whatever else you need
# you can use the standard library modules/functions as you please
# do not use any 3rd party code, except pytest if you want (see test.py)
